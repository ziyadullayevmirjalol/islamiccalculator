# Web client runtime configuration — same KEY=VALUE format as backend/.env.
# Edit this file when deploying; the JS never needs changes.
# (Named "env" without the dot because static hosts often refuse to serve dotfiles.)

# Base URL of the Go backend API (no trailing slash).
# Local dev:      http://localhost:8080
# Production:     https://islamiccalculator-api.vercel.app
API_BASE_URL=https://islamiccalculator-api.vercel.app
